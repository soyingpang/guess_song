# 更新進度 - 2026-05-21 17:40:03 HKT

## 今次需求

- 搶答題有人按搶答後，音樂要即時暫停。
- 前台和手機畫面要清楚顯示第一個按搶答的人。
- 主持判答錯後要可以補答。
- 答錯的人同一題不能自己再補答。

## 已完成

- 搶答或搶唱有人成功按下時，主持端會即時停止播放狀態、清除倒數並通知前台及手機同步播放器暫停。
- 前台畫面改為顯示「第一個搶答：名字」或「第一個搶唱：名字」。
- 手機玩家畫面改為顯示第一個搶答者，不再只寫「搶答成功」。
- 主持判「未中」後，系統會自動重新開放補答給其他玩家。
- 已答錯的玩家會被記錄為今題已嘗試，手機按鈕會鎖住，同一題不能再補答。
- 如全場已沒有可補答玩家，主持端會提示今題沒有可補答玩家。
- 更新主持端、玩家端和前台 script cache 版本，避免瀏覽器使用舊版檔案。

## 檢查

- `node --check app.js`
- `node --check player.js`
- `node --check display.js`
- `git diff --check`

## 備註

- 按照要求，今次沒有使用本機瀏覽器或 in-app browser 測試。
